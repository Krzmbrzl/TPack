# TPack Use Example

This example illustrates how you can use TPack in your CMake project, assuming it has been installed on your system. You may need to set
`CMAKE_PREFIX_PATH` accordingly in order for `find_package` to be able to locate the installed config file.
